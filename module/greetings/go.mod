// Initialized by:
// go mod init example.com/greetings
// in production code, the module path would be the URL from which your module
// can be downloaded.
// The go.mod file that identifies your code as a module that might be used from
// other code.
module example.com/greetings

go 1.15
